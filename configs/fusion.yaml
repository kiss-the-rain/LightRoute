fusion:
  type: fixed
  current_best_variant: adaptive_fusion_ablate_mlp_ocrq
  supported_types:
    - visual_only
    - text_only
    - fixed
    - rrf
  alpha: 0.5
  rrf_k: 60
  feature_dim: 16
  hidden_dim: 64
  dropout: 0.1
