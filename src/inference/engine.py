from __future__ import annotations

from typing import Any


class InferenceEngine:
    """Inference stub for future retrieval + fusion serving."""

    def __init__(self, config: dict[str, Any]) -> None:
        self.config = config
